from django.apps import AppConfig


class AdoptersConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "adopters"
    label = "pluto_adopters"
    verbose_name = "Pluto Adopters"

